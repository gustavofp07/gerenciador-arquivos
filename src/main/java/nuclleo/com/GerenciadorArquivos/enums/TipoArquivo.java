package nuclleo.com.GerenciadorArquivos.enums;

public enum TipoArquivo {
    FOTO,
    DOCUMENTO

    /*
     * public boolean isFoto() {
     * return this == FOTO;
     * }
     * 
     * public boolean isDocumento() {
     * return this == DOCUMENTO;
     * }
     */
}

/*
 * if (arquivo.getTipo().isFoto()) {
 * // processamento específico para imagens
 * }
 */